/**
 * machine-modernization controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::machine-modernization-page.machine-modernization-page"
);
