/**
 * machine-maintenance-page controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::machine-maintenance-page.machine-maintenance-page"
);
